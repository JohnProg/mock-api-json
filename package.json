{
  "name": "api-mock",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "api": "json-server db.json --port 5000 --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "json-server": "^0.12.1",
    "morgan": "^1.9.0",
    "socket.io": "^2.0.4"
  }
}
